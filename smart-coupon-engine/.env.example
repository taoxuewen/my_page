# 复制为 .env 并填入真实值。.env 不要提交到 git。

# LLM 开关：true 启用真实 Claude 文案；false 或缺省走模板 mock（项目仍可跑通）
LLM_ENABLED=false

# Anthropic API（LLM_ENABLED=true 时需要）
ANTHROPIC_API_KEY=
LLM_MODEL=claude-opus-4-8
# 如走兼容网关可改 base_url，留空用官方
ANTHROPIC_BASE_URL=
